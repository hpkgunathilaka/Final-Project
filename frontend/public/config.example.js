// Optional runtime config for deployment.
// Copy to `config.js` and set your backend API base URL.
// Example for same-origin deployment (recommended):
// window.__API_BASE__ = '/api';
// Example for separate API domain:
// window.__API_BASE__ = 'https://api.example.com/api';

window.__API_BASE__ = '/api';
